This directory contains the RFDoc development database (rfdoc.db) by default.

To initialize the database, run:

    python src/rfdoc/manage.py syncdb

To clear the database, run:

    python src/rfdoc/manage.py flush

